boolean checkPalindrome(String inputString) {
    for(int i = 0;i<inputString.length();i++){
        if(inputString.substring(i, i+1).compareTo(inputString.substring(inputString.length()-i-1, inputString.length()-i))!=0){
            return false;
        }
    }
    return true;
}
